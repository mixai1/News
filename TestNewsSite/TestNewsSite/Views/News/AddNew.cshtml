@using TestNewsSite.VeiwModels
@model NewViewModel
@{
    ViewBag.Title = Model.Title;
}

<h2>@Model.Title</h2>

@*<div class="row">
    <div class="col-md-8">
        <section id="loginForm">
            @using (Html.BeginForm("AddNew", "News", new { RedirectUrl = Model.RedirectUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
            {
                <h4>Enter New Details Info</h4>
                <hr />
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
              
                <div class="form-group">
                    @Html.LabelFor(m => m.Heading, new { @class = "col-md-2 control-label" })
                    <div class="col-md-10">
                        @Html.TextBoxFor(m => m.Heading, new { @class = "form-control" })
                        @Html.ValidationMessageFor(m => m.Heading, "", new { @class = "text-danger" })
                    </div>
                </div>
             
                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <input type="submit" value="@Model.AddButtonTitle" class="btn btn-default" />
                    </div>
                </div>
            }
        </section>
    </div>
</div>*@
